#include <stdio.h>

int main() {
    int n = 5;
    int sum;

    // Using formula: sum = n * (n + 1) / 2
    sum = 5 * (5 + 1) / 2; // sum = 5 * 6 / 2 = 30 / 2 = 15

    printf("Sum=%d\n", sum); // Output: Sum=15

    return 0;
}
